@model IEnumerable<WebApplication2.Models.Reaction>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.React)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.React)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReactId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReactId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReactId })
        </td>
    </tr>
}

</table>
